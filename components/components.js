// const Components = {
//     card(content, { col = 4, row = 1, className = '' } = {}) {
//         return `
//             <div class="card ${className}" style="grid-column: span ${col}; grid-row: span ${row};">
//                 ${content}
//             </div>
//         `;
//     },

//     title(text, { tag = 'h2', className = 'card-title' } = {}) {
//         return `<${tag} class="${className}">${text}</${tag}>`;
//     },

//     text(content, { className = 'about-text' } = {}) {
//         return `<p class="${className}">${content}</p>`;
//     },

//     skills(title, items) {
//         return `
//             <div class="skills">
//                 <h3 class="skills-title">${title}</h3>
//                 <ul class="skills-list">
//                     ${items.map(item => `<li>${item}</li>`).join('')}
//                 </ul>
//             </div>
//         `;
//     },

//   TO-DO finih this thing but im lazy